import sprite from "../../icons/sprite.svg"

export const AdultsIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-adults`}></use>
        </svg>
    )
}

export const PetrolIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-petrol`}></use>
        </svg>
    )
}

export const BedIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-bedroom`}></use>
        </svg>
    )
}

export const FavIcon    = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-fav`}></use>
        </svg>
    )
}

export const KitchenIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-kitchen`}></use>
        </svg>
    )
}

export const TransmissionIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-transmission`}></use>
        </svg>
    )
}

export const PinIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-pin`}></use>
        </svg>
    )
}

export const StarIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-star`}></use>
        </svg>
    )
}

export const ToiletIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-toilet`}></use>
        </svg>
    )
}

export const ConditionerIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-conditioner`}></use>
        </svg>
    )
}

export const RadioIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-radio`}></use>
        </svg>
    )
}

export const ShowerIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-shower`}></use>
        </svg>
    )
}

export const HobIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-hob`}></use>
        </svg>
    )
}

export const CdIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-cd`}></use>
        </svg>
    )
}

export const GasIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-gas`}></use>
        </svg>
    )
}

export const FreezerIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-freezer`}></use>
        </svg>
    )
}

export const CalendarIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-calendar`}></use>
        </svg>
    )
}

export const Van3Icon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-van3`}></use>
        </svg>
    )
}

export const Van2Icon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-van2`}></use>
        </svg>
    )
}

export const Van1Icon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-van1`}></use>
        </svg>
    )
}

export const TvIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-tv`}></use>
        </svg>
    )
}

export const WaterIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-water`}></use>
        </svg>
    )
}

export const CloseIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-close`}></use>
        </svg>
    )
}

export const MicrowaveIcon = ({ width, height }) => {
    return (
        <svg width={width} height={height}>
            <use href={`${sprite}#icon-microwave`}></use>
        </svg>
    )
}